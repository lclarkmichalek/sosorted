name: Benchmark

on:
  push:
    branches:
      - main
  pull_request:
    branches: ['**']

permissions:
  contents: write
  pull-requests: write
  deployments: write

jobs:
  benchmark:
    name: Run Benchmarks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for bench-compare

      - name: Install nightly toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-01-05

      - name: Run benchmarks
        run: cargo bench -- --output-format bencher | tee output.txt

      - name: Store benchmark result
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: sosorted Benchmarks
          tool: cargo
          output-file-path: output.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          summary-always: true
          comment-on-alert: false
          fail-on-alert: false
          benchmark-data-dir-path: dev/bench
          gh-pages-branch: gh-pages

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## Benchmark Results

            Benchmark comparison available in the [Job Summary](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).

            **[View Historical Charts](https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/dev/bench/)**

            <sub>Benchmarks run on ubuntu-latest with Rust nightly-2026-01-05</sub>
          edit-mode: replace

  # Statistical hypothesis testing for PRs
  bench-compare:
    name: Statistical Benchmark Comparison
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout sources
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Need full history for git ref comparison

      - name: Install nightly toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly-2026-01-05

      - name: Build bench-compare tool
        run: cargo build --release -p bench-compare

      - name: Run benchmark comparison
        id: bench-compare
        run: |
          set +e  # Don't exit on error
          ./target/release/bench-compare \
            --baseline ${{ github.event.pull_request.base.sha }} \
            --test ${{ github.event.pull_request.head.sha }} \
            --output html \
            --html-output benchmark-report.html \
            --sample-size 50 \
            --keep-binaries
          EXIT_CODE=$?
          echo "exit_code=$EXIT_CODE" >> $GITHUB_OUTPUT
          exit 0  # Always succeed so we can upload artifacts

      - name: Upload HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-report
          path: benchmark-report.html
          retention-days: 30

      - name: Upload Criterion Reports (Baseline)
        uses: actions/upload-artifact@v4
        with:
          name: criterion-baseline
          path: target/bench-compare-baseline/criterion/
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload Criterion Reports (Test)
        uses: actions/upload-artifact@v4
        with:
          name: criterion-test
          path: target/bench-compare-test/criterion/
          retention-days: 30
          if-no-files-found: ignore

      - name: Generate summary
        run: |
          echo "## Benchmark Comparison Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.bench-compare.outputs.exit_code }}" == "1" ]; then
            echo "⚠️ **Regressions detected!** See the HTML report for details." >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **No regressions detected.**" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📊 [Download HTML Report](../artifacts)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Configuration" >> $GITHUB_STEP_SUMMARY
          echo "- **Baseline:** \`${{ github.event.pull_request.base.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Test:** \`${{ github.event.pull_request.head.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Sample size:** 50" >> $GITHUB_STEP_SUMMARY
          echo "- **Significance level:** 0.05" >> $GITHUB_STEP_SUMMARY
          echo "- **Effect threshold:** 2.0%" >> $GITHUB_STEP_SUMMARY

      - name: Post comment with results
        uses: peter-evans/create-or-update-comment@v4
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ## Statistical Benchmark Comparison

            ${{ steps.bench-compare.outputs.exit_code == '1' && '⚠️ **Regressions detected!**' || '✅ **No regressions detected.**' }}

            📊 **[View Detailed HTML Report](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts)**

            | Baseline | Test |
            |----------|------|
            | `${{ github.event.pull_request.base.sha }}` | `${{ github.event.pull_request.head.sha }}` |

            <details>
            <summary>Statistical Testing Parameters</summary>

            - **Sample size:** 50 iterations per benchmark
            - **Significance level (α):** 0.05
            - **Effect threshold:** ≥2.0% change required
            - **Test:** Welch's t-test (two-tailed)

            </details>

            <sub>Run via bench-compare on ubuntu-latest with Rust nightly-2026-01-05</sub>
          edit-mode: replace

      - name: Fail on regressions
        if: steps.bench-compare.outputs.exit_code == '1'
        run: |
          echo "::error::Benchmark regressions detected. See the HTML report for details."
          exit 1
